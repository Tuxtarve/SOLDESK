apiVersion: v1
kind: ConfigMap
metadata:
  name: ticketing-config
  namespace: ticketing
data:
  AWS_REGION: "ap-northeast-2"
  DB_NAME: "ticketing"
  DB_PORT: "3306"
  REDIS_PORT: "6379"
  NODE_ENV: "production"
  LOG_LEVEL: "info"
---
# Secrets는 AWS Secrets Manager 또는 kubectl secret으로 관리
# kubectl create secret generic ticketing-secrets \
#   --from-literal=DB_WRITER_HOST=<rds-writer-endpoint> \
#   --from-literal=DB_READER_HOST=<rds-reader-endpoint> \
#   --from-literal=DB_USER=root \
#   --from-literal=DB_PASSWORD=dkzndk34 \
#   --from-literal=REDIS_HOST=<elasticache-endpoint> \
#   --from-literal=SQS_QUEUE_URL=<sqs-url> \
#   --from-literal=SNS_CONFIRMED_ARN=<sns-arn> \
#   --from-literal=S3_TICKETS_BUCKET=<bucket-name> \
#   --from-literal=COGNITO_USER_POOL_ID=<pool-id> \
#   --from-literal=COGNITO_CLIENT_ID=<client-id> \
#   -n ticketing
